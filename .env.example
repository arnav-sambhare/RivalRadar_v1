# Rival Radar API keys. This file is gitignored: never commit it.
# Paste each key after the "=", no quotes or spaces needed.

# LLM relevance ranking — https://console.groq.com
GROQ_API_KEY=

# EPO Open Patent Services: patents + small-startup filter — https://developers.epo.org
# (create an app there; it gives you a consumer key and a consumer secret)
EPO_OPS_KEY=
EPO_OPS_SECRET=

# Optional: avoids OpenAlex rate limits — https://openalex.org/rest-api
OPENALEX_API_KEY=
